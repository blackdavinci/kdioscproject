<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ __('invitations.link_expired_title') }} — KIDIANI OSC</title>
    <style>
        body { font-family: system-ui, sans-serif; background: #f8fafc; color: #0f172a; margin: 0; }
        .card { max-width: 26rem; margin: 4rem auto; background: #fff; padding: 2rem; border-radius: .75rem; box-shadow: 0 1px 3px rgba(0,0,0,.1); }
        h1 { font-size: 1.25rem; margin-top: 0; }
        p { color: #475569; }
    </style>
</head>
<body>
    <div class="card">
        <h1>{{ __('invitations.link_expired_title') }}</h1>
        <p>{{ __('invitations.link_expired_body') }}</p>
    </div>
</body>
</html>
